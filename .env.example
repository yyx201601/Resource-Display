# Use the pooled PostgreSQL connection string supplied by your Vercel storage integration.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Generate a long random value. The teacher Monitor sends it as a Bearer token.
MONITOR_API_KEY=replace-with-a-long-random-secret
